{{#each books as |book|}}

<ul id="book-listing" data-id="{{book.id}}">
  <li>Book ID: {{book.id}}</li>
  <li>Title: {{book.title}}</li>
  <li>Author: {{book.author}}</li>
  <li>Status: {{status.old}}</li>
</ul>
{{/each}}
